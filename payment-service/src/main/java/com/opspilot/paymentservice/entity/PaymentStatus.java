package com.opspilot.paymentservice.entity;
//n enum (short for enumeration) is a special data type used to define a fixed set of constants

public enum PaymentStatus {
    PENDING,
    SUCCESS,
    FAILED
}